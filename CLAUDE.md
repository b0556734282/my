# הנחיות לעבודה במאגר הזה

## מקור האמת הוא `main`
האתר החי נבנה מענף `main` בלבד (ראו `.github/workflows/deploy.yml`).
כל סשן חדש מתחיל מ-`main`. לכן:

**עבודה שנשארה בענף צדדי ולא מוזגה ל-`main` — לא קיימת בפועל בשביל הסשן הבא.**

זו הסיבה שנוצרה תחושה חוזרת של "הכל נמחק": העבודה לא נמחקה,
היא פשוט נשארה תלויה בענף של סשן שנסגר.

## הכלל: לסיים כל סשן במיזוג ל-main
לפני סיום עבודה, תמיד:

1. לבצע commit לכל השינויים.
2. לדחוף את ענף העבודה.
3. **למזג ל-`main`** (או לפתוח PR ולמזג אותו) — לא להשאיר את העבודה תלויה בענף.

לפני שמתחילים משהו חדש: `git log --oneline --all -20` וגם `git branch -r`,
כדי לבדוק אם כבר קיימת עבודה על אותו נושא בענף אחר. אם כן — למזג אותה, לא לבנות מחדש.

## תוכן האתר
נתוני העמותה (שם, טלפון, מייל, קישור תרומה) מרוכזים ב-`assets/js/config.js`.
לשינוי פרטים — לערוך שם, לא בכל עמוד בנפרד.

## מבנה
- `index.html` — עמוד הבית
- `donate.html` — עמוד תרומה
- `thanks.html` — עמוד תודה אחרי תרומה
- `404.html` — עמוד שגיאה
- `assets/` — עיצוב, סקריפטים ותמונות
