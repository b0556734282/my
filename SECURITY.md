# אבטחת האתר

מסמך זה מרכז את ההגנות שקיימות באתר העמותה, ואת מה שחשוב לא לשבור בעריכות עתידיות.

## מה האתר הזה כן, ומה הוא לא

האתר הוא **אתר סטטי**: קבצי HTML, CSS ותמונות בלבד, שמוגשים מ-GitHub Pages.
אין שרת, אין בסיס נתונים, אין ניהול משתמשים ואין סיסמאות.
לכן אין כאן "פריצה לחשבון" ואין מה לגנוב ממסד נתונים.

מה שכן דורש הגנה:

| הסיכון | למה זה מסוכן דווקא כאן | ההגנה באתר |
|---|---|---|
| הטמעה באתר זר (Clickjacking) | אתר מתחזה יטמיע את עמוד התרומה ויגנוב קליקים או יציג פרטי בנק מזויפים | בדיקה ב-`assets/js/security.js` + הסתרת התוכן ב-CSS |
| הזרקת קוד (XSS) | קוד זר שרץ בעמוד התרומה יכול להחליף את פרטי חשבון הבנק המוצגים | CSP נוקשה בכל עמוד + כתיבה עם `textContent` בלבד |
| הודעות `postMessage` מזויפות | אתר זר "מדבר" עם עמוד התרומה ומציג "התרומה בוצעה" בלי שנתרם דבר | אימות `event.origin` ו-`event.source` ב-`donate.js` |
| קישורים מורעלים | ערך `javascript:` בקובץ ההגדרות היה הופך לקוד רץ | `safeUrl()` עם רשימת סכימות מאושרות |
| חטיפת קישור לתרומה | `donate.html?amount=99999999` שנשלח לתורם | הגבלת הסכום לתחום שב-`config.js` |
| ספאם בטופס | רובוטים ממלאים את טופס יצירת הקשר | שדה מלכודת, מדידת זמן והגבלת קצב |
| דליפת מידע לצד שלישי | גופנים חיצוניים חושפים לגוגל מי גולש באתר העמותה | הגופנים מאוחסנים באתר עצמו |

## 1. מדיניות אבטחת תוכן (CSP)

בראש כל עמוד יש מטא־תג `Content-Security-Policy`. ברירת המחדל היא `default-src 'none'` —
כלומר "אל תטען שום דבר" — ומעליה מותר במפורש רק מה שהאתר באמת צריך:

```
default-src 'none';        אין ברירת מחדל — הכול חסום
base-uri 'none';           אי אפשר לשנות את בסיס הכתובות של העמוד
script-src 'self';         סקריפטים מהאתר בלבד. אין 'unsafe-inline' ואין 'unsafe-eval'
style-src 'self';          עיצוב מהאתר בלבד
font-src 'self';           גופנים מהאתר בלבד
img-src 'self' data:;      תמונות מהאתר, ותמונות SVG מוטבעות
connect-src 'self' https://formspree.io https://*.formspree.io;
frame-src ...              רק בעמוד התרומה, ורק נדרים פלוס
form-action 'self';        טפסים לא יכולים להישלח לאתר זר
upgrade-insecure-requests  כל בקשה נשדרגת ל-HTTPS
```

**המשמעות המעשית:** גם אם מישהו יצליח איכשהו להחדיר `<script>` לעמוד, הדפדפן פשוט יסרב להריץ אותו.

⚠ **מה לא לעשות:**
- לא להוסיף `'unsafe-inline'` או `'unsafe-eval'` ל-`script-src`.
- לא להוסיף `<script>` עם קוד בתוך ה-HTML — קוד חייב להיות בקובץ נפרד תחת `assets/js/`.
- לא להוסיף `style="..."` בתוך ה-HTML — עיצוב שייך ל-`assets/css/style.css`.
- אם מוסיפים שירות חיצוני חדש, צריך להוסיף אותו במפורש ל-CSP **בכל העמודים**.

## 2. הגנה מפני הטמעה באתר זר

`frame-ancestors` אינו נתמך בתוך מטא־תג, ולכן ההגנה ממומשת ב-`assets/js/security.js`:
אם העמוד רץ בתוך מסגרת של מקור אחר, הסקריפט מנסה לשחרר את החלון העליון,
ובמקביל מסמן `<html data-framed="true">` — וה-CSS מסתיר את כל התוכן ומציג הודעת אזהרה.
כך שגם אם הדפדפן חוסם את הניווט, אין מה לגנוב.

## 3. עמוד התרומה

- פרטי כרטיס האשראי **לא עוברים דרך האתר** ולא נשמרים בו. הסליקה מתבצעת אצל נדרים פלוס.
- הודעות `postMessage` מתקבלות רק אם:
  1. `event.origin` הוא `https://matara.pro` או `https://www.matara.pro`,
  2. `event.source` הוא בדיוק ה-iframe שלנו,
  3. התורם באמת פתח את מסך הסליקה.
- הודעות **נשלחות** למקור מפורש (`https://matara.pro`) ולא ל-`"*"`, כדי שפרטי העסקה
  לא ידלפו אם המסגרת תנווט לאתר אחר.
- "התרומה הצליחה" מתקבלת פעם אחת בלבד, ורק אחרי פתיחת הסליקה.
- כל סכום שמגיע מכתובת ה-URL מוגבל לתחום `donation.min`–`donation.max` שב-`config.js`.

## 4. טופס יצירת הקשר

בלי שרת ובלי CAPTCHA מעצבן, אבל עם שלוש שכבות:

1. **שדה מלכודת** (`website`) — מוסתר מבני אדם ומקוראי מסך. רובוט שממלא אותו מקבל
   הודעת "תודה" ולא נשלח כלום.
2. **מדידת זמן** — טופס שנשלח בפחות מ-3 שניות מרגע טעינת העמוד נדחה.
3. **הגבלת קצב** — עד 3 שליחות ב-10 דקות מאותו דפדפן.

בנוסף, כל שדה עובר ניקוי: תווי בקרה נמחקים, האורך מוגבל, ובשדות של שורה אחת גם
שורות חדשות מוסרות — כדי שלא תתאפשר הזרקת כותרות להודעת המייל.

## 5. פרטיות

- **אין** Google Analytics, אין פיקסלים, אין פרסומות, אין עוגיות מעקב.
- הגופנים מאוחסנים באתר (`assets/fonts/`) ולא נטענים משרתי גוגל, כך שכתובת ה-IP
  של הגולש לא נשלחת לצד שלישי.
- `localStorage` משמש רק לשני דברים מקומיים: בחירת מצב לילה, וספירת שליחות הטופס.
  שום מידע אישי לא נשמר ולא נשלח.
- `<meta name="referrer" content="strict-origin-when-cross-origin">` מצמצם את המידע
  שנשלח לאתרים חיצוניים.

## 6. מה שכדאי להשלים בהגדרות השרת

GitHub Pages לא מאפשר להגדיר כותרות HTTP משלנו. אם בעתיד האתר יעבור לשרת או ל-CDN
(למשל Cloudflare או Netlify), כדאי להוסיף גם את הכותרות האלה:

```
Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=()
Content-Security-Policy: ...   (אותה מדיניות, בכותרת במקום במטא־תג — כולל frame-ancestors 'none')
```

## 7. מה אסור לשים בקוד

הקבצים כאן פומביים לגמרי — כל אחד יכול לקרוא אותם בדפדפן.
**אין לשים** בקבצי האתר: סיסמאות, מפתחות API סודיים, קודי גישה לחשבון הבנק,
פרטי תורמים, או כל מידע אישי.

`nedarim.apiValid` הוא מזהה ציבורי שנדרים פלוס נותנים במיוחד להטמעה באתר — הוא לא סיסמה.
אם נדרים פלוס נותנים קוד שהם מגדירים כסודי, הוא לא הולך לכאן.

## 8. דיווח על בעיית אבטחה

הפרטים לדיווח נמצאים ב-`.well-known/security.txt`.
נשמח לקבל דיווח ולטפל בו מיד.
